name: Hello World Action
on: [push]
permissions:
      id-token: write
      contents: read 
jobs:
  GCP-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "Hello GCP"

      - name: "Authenticate to Google Cloud"
        id: auth
        uses: "google-github-actions/auth@v0"
        with:
          token_format: "access_token"
          workload_identity_provider: projects/554261169793/locations/global/workloadIdentityPools/gha-devworkshop/providers/gha-devworkshop
          service_account: ga08a2952ffea364dc369909f1286e@container-registry-09395a73.iam.gserviceaccount.com
          create_credentials_file: true
          export_environment_variables: true
          cleanup_credentials: true
          access_token_lifetime: 3600s
          access_token_scopes: https://www.googleapis.com/auth/cloud-platform
          retries: 0
          id_token_include_email: false