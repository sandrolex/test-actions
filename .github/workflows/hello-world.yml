name: GCP
on: [push]
permissions:
      id-token: write
      contents: read 
jobs:
  GCP-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "Hello GCP"

      - name: "Authenticate to Google Cloud"
        id: auth
        uses: "google-github-actions/auth@v0"
        with:
          token_format: 'access_token'
          workload_identity_provider: projects/983862741054/locations/global/workloadIdentityPools/my-pool/providers/my-provider
          service_account: my-service-account@oidc-experiment-354309.iam.gserviceaccount.com
        
      - name: "Interact with gcr read"
        uses: docker/login-action@v2
        with:
          registry: gcr.io
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - name: "Read from registry"
        run: |
          docker run gcr.io/oidc-experiment-354309/busybox cat /etc/passwd

      # - name: "docker login"
      #   run: | 
      #     gcloud auth configure-docker