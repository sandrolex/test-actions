name: Hello World Action
run-name: Test Runner Options
on: [push]
permissions:
      id-token: write
      contents: read 
jobs:
  example_matrix:
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04]
        version: [10, 12, 14]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Debug
        run: |
          echo "DEBUG"
          echo RUNNER NAME: ${{ runner.name }}
          echo STRATEGY OS ${{ strategy.matrix.os }}    

  