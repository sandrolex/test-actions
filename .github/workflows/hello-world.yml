name: Hello World Action
on: [push]
permissions:
      id-token: write
      contents: read 
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      # - uses: ./.github/workflows/composite/test-creds
      #   with:
      #     value1: "Space Invaders"
      #     account: 213174456968
      #     region: eu-west-1

  Test-OIDC:
    runs-on: ubuntu-latest
    env:
      REGION: eu-west-3
      ACCOUNT: 444838350740
    steps:
      - name: Sanity check
        run: echo "The following step should not work!"
        
      - name: Configure AWS Creds
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:iam::${{env.ACCOUNT}}:role/test_role
          aws-region: ${{env.REGION}}
      
      - name: Copy file from Bucket
        run: aws s3 cp s3://github-exp-read-bucket/filler.txt . 
      
      - name: Read local file
        run: cat filler.txt
  
